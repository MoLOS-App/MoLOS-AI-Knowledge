{
	"name": "MoLOS-AI-Knowledge",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"db:generate": "drizzle-kit generate && node scripts/normalize-migration.cjs",
		"test:unit": "vitest",
		"test": "npm run test:unit -- --run",
		"db:push": "drizzle-kit push",
		"db:studio": "drizzle-kit studio",
		"init:standalone": "node -e \"const { execSync } = require('node:child_process');const cwd = process.cwd();const marker = '/external_modules/';const idx = cwd.lastIndexOf(marker);const root = idx > -1 ? cwd.slice(0, idx) : cwd;execSync('tsx ' + root + '/scripts/init-standalone.ts', { stdio: 'inherit' });\""
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.3.1",
		"@sveltejs/kit": "^2.49.2",
		"@sveltejs/vite-plugin-svelte": "^6.2.1",
		"@types/node": "^20.0.0",
		"drizzle-kit": "^0.31.0",
		"svelte": "^5.46.1",
		"svelte-check": "^4.0.0",
		"tsx": "^4.0.0",
		"typescript": "^5.0.0",
		"vite": "^7.3.0"
	},
	"dependencies": {
		"better-sqlite3": "^12.5.0",
		"drizzle-orm": "^0.41.0",
		"lucide-svelte": "^0.561.0",
		"mdsvex": "^0.12.6",
		"vitest": "^4.0.16",
		"yaml": "^2.0.0",
		"zod": "^3.0.0"
	},
	"description": "AI prompt management, playground, humanizer, and LLM.txt storage",
	"author": "Senior Technical Product Owner"
}
